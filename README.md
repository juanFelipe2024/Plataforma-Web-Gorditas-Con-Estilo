# Plataforma-Virtual-Gorditas-Con-Estilo
Pagina web de una tienda de ropa femenina para tallas grandes.

## Configuracion segura del backend

1. Copia `Backend/.env.example` a `Backend/.env`.
2. Completa tus credenciales locales (MongoDB, JWT, Resend).
3. Nunca subas `Backend/.env` al repositorio.
